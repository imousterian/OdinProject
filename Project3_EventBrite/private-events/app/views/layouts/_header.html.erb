<nav class="navbar navbar-static-top navbar-inverse" role="navigation">
    <div class="navbar-inner">
        <div class="container">
            <ul class="nav navbar-nav">
                <li class="active"><a class="title">Mini Eventbrite</a></li>
                <li class="active"><%= link_to "All events", events_path(event_date: :all) %></li>
                <li class="active"><%= link_to "Past events", events_path(event_date: :past) %></li>
                <li class="active"><%= link_to "Upcoming events", events_path(event_date: :upcoming) %></li>
            </ul>

            <ul class="nav navbar-nav pull-right">
                <li class="active"><%=link_to "About", about_path %></li>
                <% if signed_in? %>
                    <li class="active"><%=link_to "#{current_user.name}", user_path(current_user) %></li>
                    <li class="active"><%= link_to "Logout", signout_path,
                                    method: :delete, data: { confirm: 'Are you sure?' } %>
                    </li>
                <%else%>
                    <li class="active"><%= link_to "Login", signin_path %></li>
                    <li class="active"><%= link_to "SignUp", signup_path%></li>
                <%end%>
            </ul>
        </div>
    </div>
</nav>
